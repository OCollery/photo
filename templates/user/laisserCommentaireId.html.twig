{% extends 'user/MenuUser.html.twig' %}

{% block title %}Laisser un commentaire | {{ parent() }}{% endblock %}

{% block main %}
<div class="col-xs-offset-1">
<h1>Laisser un commentaire</h1>


<div class="col-xs-offset-1">

    <p>
        Séance du {{ seanceCommente.date | date('d/m/Y') }}
    </p>

        {{ form_start(form) }}
    <div id="divLaisserCommentaire">
        <label>Laisser un commentaire: </label>
        <div>
            {{ form_widget(form) }}
        </div>
        <div>
            <button type="submit" class="btn btn-primary col-xs-3 col-xs-offset-2">Commenter</button>
        </div>
        {{ form_end(form) }}
        <form action="{{ path('commentaire') }}">
            <button class="btn btn-primary col-xs-2 col-xs-offset-1">Annuler</button>
        </form>
    </div>
</div>
<br><br>
{% endblock %}